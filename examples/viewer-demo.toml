[experiment]
name = "viewer-demo"
seed = 42

[benchmark]
name = "spacecraft-coordination-v1"

[scenario]
preset = "closed_loop_resolved"

[protocol]
name = "greedy"

[network]
latency_steps = 1
packet_loss_rate = 0.5
bandwidth_limit_per_agent = 10

[output]
directory = "../results"
